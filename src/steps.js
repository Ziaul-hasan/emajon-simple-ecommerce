/**
 * 1. Create firebase project.
 * 2. Create web app.
 * 3. npm install firebase.
 * 4. Save firebase config and export default app.
 * 5. Build > Authentication > Get started > enable signin method
 * 6. Create signup and login route.
 */

/**
 * 1. Create a context providers file. 
 * 2. Create a context and set provider.
 * 3. Create children props.
 * 4. Set the context value.
 * 5. Set the provider.
 */

/**
 * 1. npm install -g firebase-tools.
 * 2. firebase login.
 * 
 * For each project one time....
 * 1. firebase init.
 * 2. proceed.
 * 3. up and down arrow to move and spacebar to select.
 * 4. existing project. 
 * 5. select the project carefully.
 */